# 引き継ぎ書

このファイルは、Claude Code とのセッションをまたいで作業を引き継ぐための議事録です。
プロジェクトの恒久的なルールは `CLAUDE.md` を参照してください。ここには経緯・決定事項・次にやることを記録します。

## プロジェクト概要

React 製のシンプルなタスクボードアプリ。Vite + React 19 構成。

- リポジトリ: https://github.com/tobitobitobi675/task-board-samurai
- 本番サイト: https://tobitobitobi675.github.io/task-board-samurai/

## これまでの経緯（時系列）

1. **初期実装**: Vite + React の雛形を作成し、`src/App.jsx` にタスクボード機能を実装。
   - テキスト入力でタスク追加
   - チェックボックスで完了/未完了トグル
   - タスク削除
   - 完了済みタスクはグレー＋取り消し線表示
   - ブラウザで実際に操作して動作確認済み。
2. **永続化対応**: タスクをリロードしても消えないよう `localStorage` に保存する機能を追加。
   - キー: `task-board-samurai.tasks`
   - 保存: `JSON.stringify(tasks)` を `useEffect` で保存
   - 復元: 初期状態で `JSON.parse(localStorage.getItem(...))`（失敗時は空配列にフォールバック）
3. **デプロイ対応**: GitHub Pages に GitHub Actions 経由でデプロイする設定を追加。
   - `vite.config.js` に `base: '/task-board-samurai/'` を設定（GitHub Pages のプロジェクトサイトはサブパス配信のため必須）。
   - `.github/workflows/deploy.yml` を追加。`main` への push で自動ビルド・デプロイ。
   - **重要な決定事項**: リポジトリは元々 Private だったが、GitHub Free プランでは Private リポジトリに対して GitHub Pages が使えない（API が `Your current plan does not support GitHub Pages for this repository.` を返す）ことが判明。
     - ユーザーに確認の上、リポジトリを **Public に変更**して Pages を有効化する方針を選択（`gh repo edit --visibility public --accept-visibility-change-consequences` で実施）。
     - 別案（Vercel / Netlify へのデプロイでプライベートのまま運用）は不採用。今後方針を変える場合はこの決定を再検討する。
   - Pages のビルドソースは `gh api repos/.../pages -X POST -f build_type=workflow` で GitHub Actions に設定済み。
4. **ドキュメント整備**: `CLAUDE.md` に以下を追記。
   - デプロイ先の情報（URL・自動デプロイの仕組み）
   - 技術スタック（Vite / React 19 / JavaScript / oxlint / localStorage / GitHub Actions）
   - コンポーネントの命名規約（PascalCase のコンポーネント、camelCase のハンドラ、kebab-case の CSS クラス、1 コンポーネント 1 ファイル方針）

## 現状のファイル構成

- `src/App.jsx`: タスクボードのメインコンポーネント（状態管理・localStorage 永続化を含む）
- `src/App.css`: タスクボードのスタイル
- `src/main.jsx`: エントリーポイント
- `.github/workflows/deploy.yml`: GitHub Pages への自動デプロイ用ワークフロー
- `vite.config.js`: `base` にリポジトリ名を設定済み

## 次回以降の作業候補（未着手）

- コンポーネントが増えてきた場合の `src/components/` への切り出し（現状は `App.jsx` 一本）
- テストコードは未整備（ユニットテスト・E2E とも未導入）
- oxlint は導入済みだが CI では未実行（`npm run lint` を Actions に組み込むかは未決定）
- タスクの並び替え・編集機能など、追加機能の要望が出た場合はここに追記していく

## 運用ルール（再掲・重要点のみ）

- コード変更ごとにコミット＆プッシュする（`CLAUDE.md` の Git 運用ルールを参照）。
- force push やブランチ操作など破壊的な操作、リポジトリ設定変更（今回の Public 化など）は事前にユーザーに確認する。
